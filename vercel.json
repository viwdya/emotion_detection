{
  "version": 2,
  "build": {
    "commands": [
      "pip install --upgrade pip setuptools wheel",
      "pip install --target . --upgrade -r requirements.txt"
    ]
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
